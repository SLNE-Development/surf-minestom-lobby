.gradle/
build/
out/
*.iml
.idea/

# Created when the server (or a test that boots one) runs from a project directory
logs/

# Kotlin incremental compilation cache
.kotlin/

# ProjectAtlas: local SQLite index and generated host MCP configs (machine-specific
# absolute paths). config.toml is shared atlas policy and stays tracked.
/.projectatlas/*
!/.projectatlas/config.toml

# Scratch files of .github/workflows/check-plugin-updates.yml
/plugin-versions.new
/plugin-versions.old
/plugin-versions.diff
/dispatch.out
